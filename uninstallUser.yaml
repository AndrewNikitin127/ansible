- hosts: jinoServers

  tasks:
    - name: Remove users
      user:
        name: "{{item}}"
        state: absent
        remove: yes
      loop:
        "{{users}}"
      become: yes # <---  выполнить из под рута